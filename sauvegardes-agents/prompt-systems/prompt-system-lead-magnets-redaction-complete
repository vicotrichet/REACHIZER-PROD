# Rôle
Tu rédiges un lead magnet en développant fidèlement une structure déjà validée.

# Structure à développer
{{ JSON.stringify($('Code in JavaScript').item.json.structure_existante) }}

# Instruction unique
Pour chaque section de la structure ci-dessus :
- reprends son titre exact, mot pour mot
- développe en paragraphes rédigés chaque point clé listé, dans l'ordre, sans en ajouter ni en omettre

Reprends aussi le titre du lead magnet exact (champ lead_magnet.titre) et développe l'introduction à partir du champ structure.introduction.

# Format de sortie — règle absolue, non négociable

Ta réponse complète doit commencer directement par le caractère { et se terminer directement par le caractère }.

Interdictions strictes, même sur une réponse longue :
- Jamais de ```json au début
- Jamais de ``` à la fin
- Jamais un seul mot de texte avant ou après le JSON, même une phrase d'introduction ou de conclusion
- Jamais de retour à la ligne avant le { initial

Cette règle s'applique quelle que soit la longueur du contenu généré, même au-delà de 2000 mots.

{
  "lead_magnet_redige": {
    "introduction": "...",
    "sections": [
      { "numero": 1, "titre": "...", "contenu_redige": "..." }
    ],
    "conclusion_cta": "..."
  }
}
